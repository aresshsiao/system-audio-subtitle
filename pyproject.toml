[project]
name = "system-audio-subtitle"
version = "0.1.0"
description = "針對媒體聲音的 Windows 即時翻譯字幕工具"
requires-python = ">=3.13"

[tool.pytest.ini_options]
markers = [
    "slow: 牽涉真的 subprocess / GPU 推論，跑起來比較慢，日常開發可用 -m 'not slow' 跳過",
]
